name: 'coverage'
on:
  pull_request:
    branches:
      - master
      - develop
jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: npx jest --coverage --json --outputFile=report.json
      - uses: ArtiomTr/jest-coverage-report-action@v2
        continue-on-error: true
        with:
          skip-step: all
          coverage-file: report.json
