# Session Handover — 2026-05-24

## What Was Done

- Ran `/init` to create `CLAUDE.md` at the repo root with commands, architecture, tooling, and project conventions
- Removed the `_docs/` implementation-log convention (used with Gemini CLI, no longer needed) from `CLAUDE.md`; asked user to delete `_docs/` directory manually via `! rm -rf _docs` (Claude lacks shell permission for `rm -rf`)
- Conducted a comprehensive project audit across three parallel Explore agents covering:
  - Dependency versions and compatibility
  - Tool configurations (ESLint, Prettier, Stylelint, Markuplint, TypeScript, Jest, Storybook, Husky, lint-staged, CI, Vercel)
  - Next.js App Router code quality, Docker setup, i18n, middleware, performance, accessibility
- Verified two critical findings directly by reading source files:
  - `docker-compose.yml:14-16` — live API tokens hard-coded and committed
  - `src/middleware.ts:32` — duplicate `export default` silently overrides the named `middleware` export (custom headers are dead code)
- Created `docs/audit.md` with 30 prioritised action items (Critical/High/Medium/Low) in checkbox format for gradual remediation

## Decisions Made

- Audit report placed at `docs/audit.md` (new `docs/` directory; user's choice)
- Format: Critical/High/Medium/Low labels + `- [ ]` checkboxes for progress tracking
- Report is read-only analysis only — no code changes made in this session
- `CLAUDE.md` includes session handover instructions (read `.claude/handovers/` at session start, prompt `/handover` at end)

## Discarded Options

- `.github/PROJECT_AUDIT.md` and root-level `IMPROVEMENTS.md` — user preferred `docs/audit.md`
- Numbered-priority TOP 10 + detail section format — user preferred label-based grouping
- Making code fixes in the same session — deferred; user wants to address items incrementally

## Blockers and Gotchas

- `_docs/` directory deletion was blocked (no `rm -rf` permission) — user must run `! rm -rf /Users/bear29ers/Developer/bear29ers.com/_docs` manually
- The middleware bug (`middleware.ts:32`) is subtle: Next.js uses the `default` export, so the entire named `middleware` function (including all custom header logic) is unreachable in production. The `generateMetadata` in `[locale]/layout.tsx` silently gets no headers back.
- context7 `resolve-library-id` calls initially failed due to missing required `query` parameter (parameter name differs from the tool description). Resolved by loading the schema via `ToolSearch`.

## Learnings

- `docker-compose.yml` contains real, live secrets committed to the repository — this is a genuine security incident requiring immediate token rotation and git history rewriting
- `markuplintrc` has a JSON syntax error (missing comma at L20), meaning markuplint rules have likely never been enforced
- `eslint-plugin-storybook` is installed but not wired into the flat config — ESLint does not actually lint Storybook files
- `ts-jest` and `ts-node` are installed but unused — `next/jest` uses SWC
- `next lint` (used in `package.json` and lint-staged) will be removed in Next.js 16

## Next Steps

1. **[Critical]** Rotate Instagram Graph API token and Newt CDN API token; remove from git history with BFG or `git filter-repo`; move to `.env` (gitignored)
2. **[Critical]** Fix `src/middleware.ts` — remove `export default createMiddleware(routing)` on line 32; ensure request headers propagation uses `NextResponse.next({ request: { headers } })` pattern
3. **[High]** Delete `_docs/` directory (`! rm -rf _docs`)
4. **[High]** Add production Dockerfile stages (`builder` + `runner` with `output: 'standalone'`, `npm ci`, non-root user, `node:22-alpine`)
5. **[High]** Pin Node version: `Dockerfile` → `node:22-slim`, add `.nvmrc`, add `engines` to `package.json`
6. **[High]** Add `typecheck` npm script and CI lint/build/typecheck workflow
7. Work through remaining items in `docs/audit.md` by priority

## Related Files

- `CLAUDE.md` — created (repo root)
- `docs/audit.md` — created (30-item audit report)
- `docker-compose.yml` — read (confirmed leaked tokens at :14-16)
- `src/middleware.ts` — read (confirmed duplicate export bug at :32)
- `GEMINI.md` — read (source for previous Gemini CLI conventions)
- `package.json` — read (dependency versions)
- `eslint.config.mjs` — read (tooling audit)
- `tsconfig.json` — read (tooling audit)
- `.github/workflows/` — read (CI gap analysis)
- `Dockerfile`, `docker-compose.yml` — read (Docker audit)
