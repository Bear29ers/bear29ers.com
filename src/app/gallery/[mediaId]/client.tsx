'use client';

import type { FC } from 'react';

const GalleryDetailClient: FC = () => {
  return (
    <div className="min-h-screen w-full flex-center">
      <p>GalleryDetailClient</p>
    </div>
  );
};

export default GalleryDetailClient;
